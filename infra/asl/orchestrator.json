{
  "Comment": "Replace with actual workflow. Template vars available: ${glue_database_name}, ${glue_table_name}, ${s3_input_path}, ${s3_output_path}. IMPORTANT: escape Step Functions JSONPath $.field as $${field} to avoid Terraform templatefile conflicts.",
  "StartAt": "Step1",
  "States": {
    "Step1": {
      "Type": "Pass",
      "Result": {
        "database": "${glue_database_name}",
        "table": "${glue_table_name}",
        "input_path": "${s3_input_path}"
      },
      "Next": "Step2"
    },
    "Step2": {
      "Type": "Pass",
      "Result": {
        "status": "step2_complete"
      },
      "End": true
    }
  }
}
